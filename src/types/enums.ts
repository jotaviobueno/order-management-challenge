export enum OrderState {
  CREATED = "CREATED",
  ANALYSIS = "ANALYSIS",
  COMPLETED = "COMPLETED",
}

export enum OrderStatus {
  ACTIVE = "ACTIVE",
  DELETED = "DELETED",
}

export enum ServiceStatus {
  PENDING = "PENDING",
  DONE = "DONE",
}
